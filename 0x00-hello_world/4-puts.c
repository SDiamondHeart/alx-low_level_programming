#include <stdio.h>
/**
 * main - Entry point
 *
 * Return : Always 0 - (Success)
 */
int main(void)
/**
 * return : value of main 0
 */
{
	puts("\"Programming is like building a multilingual puzzle");
	/**
	 * return: value for main() shows how the program exited
	 */
	return (0);
}
